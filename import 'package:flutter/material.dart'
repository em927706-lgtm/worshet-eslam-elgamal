;
void main() => runApp(const MyApp());
class MyApp extends StatelessWidget {
  const MyApp({super.key});
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'ورشة اسلام الجمال',
      debugShowCheckedModeBanner: false,
      home: Scaffold(
        appBar: AppBar(title: const Text('ورشة دهانات اسلام الجمال'), centerTitle: true),
        body: const Center(child: Text('مرحبا بيك في التطبيق', style: TextStyle(fontSize: 24))),
      ),
